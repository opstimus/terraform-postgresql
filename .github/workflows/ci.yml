name: Terraform CI

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  lint:
    name: "Lint"
    uses: opstimus/github-actions-terraform/.github/workflows/lint.yml@v1
